meta {
  name: Cancel Paid Registration
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/features/v1/registrations/{{crudRegistrationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{pesertaAccessToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}

