meta {
  name: Root Deletes Membership Test User (Cleanup)
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/core/v1/users/{{crudMembershipUserId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
