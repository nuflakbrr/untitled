/**
 * SITIVENT API Test Suite - Global Pre-request Script
 * Auto-login and attach token before any authenticated request.
 * 
 * This runs before every request in the collection.
 * Token is stored in `accessToken` env var after first successful login.
 */
