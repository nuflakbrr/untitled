meta {
  name: Delete Article Category
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/features/v1/article-categories/{{crudArticleCategoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{rootAccessToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
