meta {
  name: List Article Categories (Public)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/features/v1/article-categories
  body: none
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
  test("returns array", function() { expect(res.body.data).to.be.an("array").that.is.not.empty; });
}
