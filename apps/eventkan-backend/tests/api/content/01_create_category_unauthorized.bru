meta {
  name: Create Article Category Requires Auth
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/features/v1/article-categories
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Prestasi"
  }
}

tests {
  test("status 401", function() { expect(res.status).to.equal(401); });
}
