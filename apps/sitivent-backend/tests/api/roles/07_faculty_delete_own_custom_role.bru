meta {
  name: Faculty Deletes Own Custom Role (Cleanup)
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/core/v1/roles/{{crudFasilkomRoleId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{fasilkomAccessToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
