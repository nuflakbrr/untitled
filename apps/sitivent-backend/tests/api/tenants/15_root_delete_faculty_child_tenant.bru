meta {
  name: Root Deletes Faculty Child Tenant (Cleanup)
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/core/v1/tenants/{{crudFacultyChildTenantId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("status 200", function() { expect(res.status).to.equal(200); });
}
