meta {
  name: Cross-Tenant Payment Gateway Read Is Forbidden
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/core/v1/tenants/{{crudFacultyChildTenantId}}/payment-gateway
  body: none
  auth: bearer
}

auth:bearer {
  token: {{teknikAccessToken}}
}

tests {
  test("status 403", function() { expect(res.status).to.equal(403); });
}
