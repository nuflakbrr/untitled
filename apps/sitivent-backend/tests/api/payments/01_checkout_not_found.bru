meta {
  name: Checkout Non-Existent Registration
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/features/v1/payments/checkout
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{pesertaAccessToken}}
}

body:json {
  {
    "registration_id": "11111111-1111-4111-8111-111111111111"
  }
}

tests {
  test("status 404", function() { expect(res.status).to.equal(404); });
}
